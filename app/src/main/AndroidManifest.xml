<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.antoni.wijaya.thesport">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.team.search.SearchTeamActivity"
            android:theme="@style/AppThemeNoAction">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".ui.team.DetailTeamActivity" />
        <activity android:name=".ui.favorite.FavoriteActivity" />
        <activity
            android:name=".ui.match.search.SearchMatchActivity"
            android:theme="@style/AppThemeNoAction">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".ui.match.DetailMatchActivity" />
        <activity
            android:name=".ui.detail.activity.DetailLeagueActivity"
            android:label="@string/title_activity_detail_league"
            android:theme="@style/AppThemeNoAction">

        </activity>
        <activity android:name=".ui.home.activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>